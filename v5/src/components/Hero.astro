---
// Hero Section - Typography-First Design
---

<section class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <h1 class="hero-headline">
        <span class="line line-1">Find good problems.</span>
        <span class="line line-2">Make good apps.</span>
      </h1>
      <p class="hero-subheadline">
        We help startups and SMEs discover what their business really needs—before writing a single line of code.
      </p>
      <div class="hero-cta">
        <a href="#contact" class="btn btn-primary">
          <span>Book Your Free Brief</span>
          <i class="fas fa-arrow-right"></i>
        </a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="visual-element">
        <div class="circle circle-1"></div>
        <div class="circle circle-2"></div>
        <div class="circle circle-3"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 80px;
    background-color: var(--color-white);
    position: relative;
    overflow: hidden;
  }

  .hero-container {
    max-width: var(--max-width-content);
    margin: 0 auto;
    padding: var(--space-3xl) var(--space-xl);
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero-headline {
    font-size: clamp(2.5rem, 6vw, var(--text-5xl));
    line-height: var(--leading-tight);
    margin-bottom: var(--space-2xl);
    color: var(--color-navy);
  }

  .line {
    display: block;
  }

  .line-1 {
    animation: fadeInUp 0.7s ease-out 0.1s both;
  }

  .line-2 {
    color: var(--color-terracotta);
    animation: fadeInUp 0.7s ease-out 0.3s both;
  }

  .hero-subheadline {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-light);
    max-width: 480px;
    margin-bottom: var(--space-2xl);
    animation: fadeInUp 0.7s ease-out 0.5s both;
  }

  .hero-cta {
    animation: fadeInUp 0.7s ease-out 0.7s both;
  }

  .hero-visual {
    position: relative;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .visual-element {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .circle {
    position: absolute;
    border-radius: 50%;
    opacity: 0.8;
  }

  .circle-1 {
    width: 400px;
    height: 400px;
    background: linear-gradient(135deg, var(--color-cyan) 0%, rgba(100, 181, 246, 0.2) 100%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: float 6s ease-in-out infinite;
  }

  .circle-2 {
    width: 200px;
    height: 200px;
    background: linear-gradient(135deg, var(--color-terracotta) 0%, rgba(208, 120, 85, 0.3) 100%);
    top: 10%;
    right: 10%;
    animation: float 5s ease-in-out infinite 0.5s;
  }

  .circle-3 {
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, var(--color-navy) 0%, rgba(26, 58, 82, 0.4) 100%);
    bottom: 15%;
    left: 10%;
    animation: float 4s ease-in-out infinite 1s;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(-50%, -50%) translateY(0);
    }
    50% {
      transform: translate(-50%, -50%) translateY(-20px);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 968px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
      text-align: center;
    }

    .hero-subheadline {
      max-width: 100%;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-visual {
      height: 300px;
      order: -1;
    }

    .circle-1 {
      width: 280px;
      height: 280px;
    }

    .circle-2 {
      width: 140px;
      height: 140px;
    }

    .circle-3 {
      width: 80px;
      height: 80px;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding-top: 60px;
    }

    .hero-container {
      padding: var(--space-2xl) var(--space-lg);
    }

    .hero-headline {
      font-size: var(--text-2xl);
    }

    .hero-visual {
      height: 200px;
    }
  }
</style>
