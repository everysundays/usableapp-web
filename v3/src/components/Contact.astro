---
// Contact Section - Creative Interaction
---

<section class="contact">
  <div class="contact-content">
    <div class="section-header">
      <h2 class="section-title">READY TO FIND THE RIGHT PROBLEM?</h2>
      <p class="section-intro">Let's discover what your business needs.</p>
    </div>

    <div class="contact-card-wrapper">
      <div class="contact-card">
        <div class="card-header">
          <div class="logo-circle"></div>
          <div class="company-info">
            <h3>Usable App Co.</h3>
            <p class="tagline">Find good problems. Make good apps.</p>
          </div>
        </div>

        <div class="contact-details">
          <div class="contact-item">
            <span class="contact-label">Email</span>
            <a href="mailto:contact@usableapp.co" class="contact-value">contact@usableapp.co</a>
          </div>

          <div class="contact-item">
            <span class="contact-label">Phone</span>
            <a href="tel:+66618317773" class="contact-value">+66 61 831 7773</a>
          </div>

          <div class="contact-item">
            <span class="contact-label">Address</span>
            <p class="contact-value">36 Thawiwattana 47<br/>Thawiwattana, Bangkok 10170</p>
          </div>
        </div>

        <div class="card-footer">
          <p class="cta">Start your app's day-1 with us.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(155, 89, 182, 0.03) 0%, rgba(52, 152, 219, 0.03) 100%);
    padding: 0;
    position: relative;
  }

  .contact-content {
    width: 100%;
    max-width: 1200px;
    padding: 0 var(--space-3xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
    height: 100%;
    max-height: calc(100vh - 80px);
    justify-content: center;
    align-items: center;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  .section-title {
    font-size: clamp(40px, 5vw, 80px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    text-transform: uppercase;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .section-intro {
    font-size: clamp(18px, 2.2vw, 24px);
    color: var(--color-text-light);
    margin: 0;
    font-weight: 200;
  }

  .contact-card-wrapper {
    width: 100%;
    max-width: 600px;
    perspective: 1000px;
  }

  .contact-card {
    padding: var(--space-4xl);
    border: 2px solid rgba(255, 107, 157, 0.3);
    background: rgba(255, 255, 255, 0.7);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(10px);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .contact-card::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(135deg, rgba(255, 107, 157, 0.05) 0%, rgba(255, 142, 83, 0.05) 100%);
    border-radius: 50%;
    transform: scale(0);
    transition: transform 0.6s ease;
  }

  .contact-card:hover {
    transform: translateY(-12px) rotate(1deg);
    border-color: var(--color-purple);
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 24px 64px rgba(255, 107, 157, 0.3);
  }

  .contact-card:hover::before {
    transform: scale(1);
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: var(--space-xl);
    margin-bottom: var(--space-3xl);
    padding-bottom: var(--space-xl);
    border-bottom: 2px solid rgba(255, 107, 157, 0.2);
    position: relative;
    z-index: 1;
  }

  .logo-circle {
    width: 80px;
    height: 80px;
    border: 3px solid var(--color-pink);
    border-radius: 50%;
    background: transparent;
    flex-shrink: 0;
    position: relative;
    box-shadow: 0 0 20px rgba(255, 107, 157, 0.3);
  }

  .logo-circle::before {
    content: '';
    position: absolute;
    inset: -6px;
    border: 2px solid var(--color-orange);
    border-radius: 50%;
    opacity: 0.5;
    animation: rotate 10s linear infinite;
  }

  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .company-info {
    flex: 1;
  }

  .company-info h3 {
    font-size: clamp(28px, 3.5vw, 48px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .tagline {
    font-size: clamp(16px, 2vw, 22px);
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0;
    font-weight: 200;
  }

  .contact-details {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    margin-bottom: var(--space-3xl);
    position: relative;
    z-index: 1;
  }

  .contact-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    padding-left: var(--space-xl);
    position: relative;
  }

  .contact-item::before {
    content: '';
    position: absolute;
    left: 0;
    top: 10px;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    box-shadow: 0 0 10px rgba(255, 107, 157, 0.5);
  }

  .contact-label {
    font-size: clamp(12px, 1.2vw, 14px);
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    font-weight: 200;
  }

  .contact-value {
    font-size: clamp(18px, 2vw, 24px);
    color: var(--color-text);
    text-decoration: none;
    font-weight: 200;
    line-height: 1.6;
    transition: all 0.3s ease;
  }

  .contact-value:hover {
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    transform: translateX(8px);
  }

  .card-footer {
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .cta {
    font-size: clamp(20px, 2.5vw, 28px);
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0;
    font-weight: 200;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @media (max-width: 768px) {
    .contact-card {
      padding: var(--space-2xl);
    }

    .card-header {
      flex-direction: column;
      text-align: center;
      gap: var(--space-lg);
    }

    .logo-circle {
      margin: 0 auto;
    }

    .contact-content {
      padding: 0 var(--space-xl);
      max-height: calc(100vh - 80px);
    }
  }
</style>
