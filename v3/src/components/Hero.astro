---
// Hero Section - Experimental Typography
---

<section class="hero">
  <div class="hero-content">
    <div class="hero-text-container">
      <h1 class="hero-title">
        <span class="word word-1">Find</span>
        <span class="word word-2">Good</span>
        <span class="word word-3 accent">Problems.</span>
      </h1>
      <h1 class="hero-title hero-title-2">
        <span class="word word-4">Make</span>
        <span class="word word-5">Usable</span>
        <span class="word word-6 accent">Apps.</span>
      </h1>
    </div>
    <div class="hero-subtitle-container">
      <p class="hero-subtitle">
        Discover what your business really needs—before writing code.
      </p>
    </div>
  </div>
</section>

<style>
  .hero {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-base);
    padding: 0;
    position: relative;
    overflow: hidden;
  }

  .hero-content {
    width: 100%;
    max-width: 1600px;
    padding: 0 var(--space-4xl);
    position: relative;
    z-index: 2;
  }

  .hero-text-container {
    position: relative;
    margin-bottom: var(--space-3xl);
  }

  .hero-title {
    font-size: clamp(64px, 10vw, 160px);
    font-weight: 200;
    line-height: 0.85;
    letter-spacing: -0.05em;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: var(--space-md);
    position: relative;
  }

  .hero-title-2 {
    margin-top: var(--space-lg);
    padding-left: clamp(80px, 15vw, 200px);
  }

  .word {
    display: inline-block;
    color: var(--color-text);
    transition: all 0.3s ease;
    cursor: default;
    position: relative;
  }

  .word:hover {
    transform: translateY(-8px) scale(1.05);
  }

  .word.accent {
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 50%, var(--color-purple) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
  }

  .word.accent::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 50%, var(--color-purple) 100%);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .word.accent:hover::after {
    transform: scaleX(1);
  }

  .word-1 {
    animation: fadeInUp 0.6s ease-out 0.1s both;
  }

  .word-2 {
    animation: fadeInUp 0.6s ease-out 0.2s both;
  }

  .word-3 {
    animation: fadeInUp 0.6s ease-out 0.3s both;
  }

  .word-4 {
    animation: fadeInUp 0.6s ease-out 0.4s both;
  }

  .word-5 {
    animation: fadeInUp 0.6s ease-out 0.5s both;
  }

  .word-6 {
    animation: fadeInUp 0.6s ease-out 0.6s both;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-subtitle-container {
    max-width: 700px;
    margin-left: clamp(120px, 20vw, 300px);
    position: relative;
  }

  .hero-subtitle {
    font-size: clamp(20px, 2.5vw, 32px);
    font-weight: 200;
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.6;
    position: relative;
    animation: fadeInUp 0.8s ease-out 0.8s both;
  }

  .hero-subtitle::before {
    content: '';
    position: absolute;
    left: -40px;
    top: 50%;
    transform: translateY(-50%);
    width: 24px;
    height: 2px;
    background: linear-gradient(90deg, var(--color-pink) 0%, var(--color-orange) 100%);
  }

  @media (max-width: 968px) {
    .hero-title-2 {
      padding-left: clamp(40px, 10vw, 120px);
    }

    .hero-subtitle-container {
      margin-left: clamp(60px, 15vw, 200px);
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      padding: 0 var(--space-xl);
    }

    .hero-title {
      font-size: clamp(48px, 12vw, 96px);
      gap: var(--space-sm);
    }

    .hero-title-2 {
      padding-left: 0;
      margin-top: var(--space-md);
    }

    .hero-subtitle-container {
      margin-left: 0;
    }

    .hero-subtitle::before {
      display: none;
    }
  }
</style>
