---
// Showcase Section - Creative Project Presentation
---

<section class="showcase">
  <div class="showcase-content">
    <div class="section-header">
      <h2 class="section-title">PROBLEMS SOLVED. APPS THAT WORK.</h2>
      <p class="section-intro">Real discoveries. Real results.</p>
    </div>

    <div class="projects-container">
      <div class="project-card project-1">
        <div class="project-header">
          <div class="project-number">01</div>
          <h3 class="project-title">Project Name 1</h3>
        </div>
        <div class="project-body">
          <div class="project-detail">
            <span class="detail-label">Challenge</span>
            <p>Brief problem statement that the client faced</p>
          </div>
          <div class="project-detail">
            <span class="detail-label">Discovery</span>
            <p>What we uncovered through our research and PoCs</p>
          </div>
          <div class="project-detail">
            <span class="detail-label">Result</span>
            <p>Business outcome and impact achieved</p>
          </div>
        </div>
      </div>

      <div class="project-card project-2">
        <div class="project-header">
          <div class="project-number">02</div>
          <h3 class="project-title">Project Name 2</h3>
        </div>
        <div class="project-body">
          <div class="project-detail">
            <span class="detail-label">Challenge</span>
            <p>Brief problem statement that the client faced</p>
          </div>
          <div class="project-detail">
            <span class="detail-label">Discovery</span>
            <p>What we uncovered through our research and PoCs</p>
          </div>
          <div class="project-detail">
            <span class="detail-label">Result</span>
            <p>Business outcome and impact achieved</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .showcase {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(52, 152, 219, 0.03) 0%, rgba(155, 89, 182, 0.03) 100%);
    padding: 0;
    position: relative;
  }

  .showcase-content {
    width: 100%;
    max-width: 1400px;
    padding: 0 var(--space-3xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
    height: 100%;
    max-height: calc(100vh - 80px);
    justify-content: center;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  .section-title {
    font-size: clamp(40px, 5vw, 80px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    text-transform: uppercase;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .section-intro {
    font-size: clamp(18px, 2.2vw, 24px);
    color: var(--color-text-light);
    margin: 0;
    font-weight: 200;
  }

  .projects-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    align-items: start;
  }

  .project-card {
    padding: var(--space-3xl);
    border: 1px solid rgba(255, 107, 157, 0.2);
    background: rgba(255, 255, 255, 0.6);
    border-radius: var(--radius-lg);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    backdrop-filter: blur(10px);
    cursor: pointer;
  }

  .project-card:hover {
    transform: translateY(-12px) rotate(-1deg);
    border-color: var(--color-purple);
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 20px 60px rgba(255, 107, 157, 0.25);
  }

  .project-1 {
    margin-top: 0;
  }

  .project-2 {
    margin-top: var(--space-2xl);
  }

  .project-header {
    display: flex;
    align-items: baseline;
    gap: var(--space-lg);
    margin-bottom: var(--space-2xl);
    padding-bottom: var(--space-lg);
    border-bottom: 2px solid rgba(255, 107, 157, 0.2);
  }

  .project-number {
    font-size: clamp(32px, 4vw, 64px);
    font-weight: 200;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    flex-shrink: 0;
  }

  .project-title {
    font-size: clamp(24px, 3vw, 40px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    line-height: 1.2;
    flex: 1;
  }

  .project-body {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }

  .project-detail {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    position: relative;
    padding-left: var(--space-lg);
  }

  .project-detail::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
  }

  .detail-label {
    font-size: clamp(12px, 1.2vw, 14px);
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    font-weight: 200;
  }

  .project-detail p {
    font-size: clamp(16px, 1.8vw, 20px);
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.6;
    font-weight: 200;
  }

  @media (max-width: 968px) {
    .projects-container {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }

    .project-2 {
      margin-top: 0;
    }

    .showcase-content {
      gap: var(--space-xl);
      padding: 0 var(--space-xl);
      max-height: calc(100vh - 80px);
    }
  }

  @media (max-width: 768px) {
    .project-header {
      flex-direction: column;
      gap: var(--space-sm);
    }

    .project-card {
      padding: var(--space-2xl);
    }
  }
</style>
