---
// Pricing Section - Creative Engagement Flow
---

<section class="pricing">
  <div class="pricing-content">
    <div class="section-header">
      <h2 class="section-title">START YOUR APP'S DAY-1 WITH US.</h2>
      <p class="section-intro">Two meetings. One clear path forward.</p>
    </div>

    <div class="engagement-container">
      <div class="meeting-card meeting-1">
        <div class="meeting-header">
          <div class="meeting-number">1</div>
          <div class="meeting-title-group">
            <h3 class="meeting-title">Free Brief</h3>
            <p class="meeting-subtitle">Your Story</p>
          </div>
        </div>
        <ul class="meeting-list">
          <li>No cost, no commitment</li>
          <li>NDA available</li>
          <li>60-90 minutes</li>
        </ul>
      </div>

      <div class="flow-connector">
        <div class="connector-line"></div>
        <div class="connector-arrow">→</div>
      </div>

      <div class="meeting-card meeting-2 highlight">
        <div class="meeting-header">
          <div class="meeting-number">2</div>
          <div class="meeting-title-group">
            <h3 class="meeting-title">The Proposal</h3>
            <p class="meeting-subtitle">Our Plan</p>
          </div>
        </div>
        <ul class="meeting-list">
          <li>Discovery roadmap</li>
          <li>Timeline breakdown</li>
          <li>Custom PoC plan</li>
        </ul>
      </div>
    </div>

    <div class="outcome-container">
      <div class="outcome-card outcome-1">
        <h4>The Work</h4>
        <p>Time-based contract. Typically 5-7 PoCs to reach solid requirements.</p>
      </div>
      <div class="outcome-card outcome-2 highlight">
        <h4>The Outcome</h4>
        <p>Business Requirements Specification with KPIs—your development team's north star.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .pricing {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(255, 142, 83, 0.03) 0%, rgba(255, 107, 157, 0.03) 100%);
    padding: 0;
    position: relative;
  }

  .pricing-content {
    width: 100%;
    max-width: 1400px;
    padding: 0 var(--space-3xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
    height: 100%;
    max-height: calc(100vh - 80px);
    justify-content: center;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  .section-title {
    font-size: clamp(40px, 5vw, 80px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
    text-transform: uppercase;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .section-intro {
    font-size: clamp(18px, 2.2vw, 24px);
    color: var(--color-text-light);
    margin: 0;
    font-weight: 200;
  }

  .engagement-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2xl);
    margin: var(--space-2xl) 0;
    position: relative;
  }

  .meeting-card {
    flex: 1;
    max-width: 400px;
    padding: var(--space-3xl);
    border: 1px solid rgba(255, 107, 157, 0.2);
    background: rgba(255, 255, 255, 0.6);
    border-radius: var(--radius-lg);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(10px);
    cursor: pointer;
  }

  .meeting-card:hover {
    transform: translateY(-10px) rotate(0.5deg);
    border-color: var(--color-pink);
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 16px 48px rgba(255, 107, 157, 0.2);
  }

  .meeting-card.highlight {
    background: linear-gradient(135deg, rgba(255, 107, 157, 0.1) 0%, rgba(255, 142, 83, 0.1) 100%);
    border-color: var(--color-purple);
    border-width: 2px;
    transform: scale(1.05);
  }

  .meeting-card.highlight:hover {
    transform: scale(1.08) translateY(-10px) rotate(0.5deg);
  }

  .meeting-1 {
    margin-right: var(--space-xl);
  }

  .meeting-2 {
    margin-left: var(--space-xl);
  }

  .meeting-header {
    display: flex;
    align-items: flex-start;
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
    padding-bottom: var(--space-lg);
    border-bottom: 2px solid rgba(255, 107, 157, 0.2);
  }

  .meeting-number {
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid var(--color-pink);
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(255, 107, 157, 0.1) 0%, rgba(255, 142, 83, 0.1) 100%);
    font-size: var(--text-2xl);
    font-weight: 200;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    flex-shrink: 0;
    position: relative;
  }

  .meeting-number::before {
    content: '';
    position: absolute;
    inset: -4px;
    border: 2px solid var(--color-pink);
    border-radius: 50%;
    opacity: 0.3;
  }

  .meeting-title-group {
    flex: 1;
  }

  .meeting-title {
    font-size: clamp(24px, 3vw, 36px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
    line-height: 1.2;
  }

  .meeting-subtitle {
    font-size: clamp(14px, 1.5vw, 18px);
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 200;
    margin: 0;
  }

  .meeting-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .meeting-list li {
    font-size: clamp(16px, 1.8vw, 20px);
    color: var(--color-text-light);
    padding-left: var(--space-xl);
    position: relative;
    font-weight: 200;
    line-height: 1.5;
  }

  .meeting-list li::before {
    content: '→';
    position: absolute;
    left: 0;
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 200;
  }

  .flow-connector {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    min-width: 80px;
  }

  .connector-line {
    width: 2px;
    height: 40px;
    background: linear-gradient(180deg, var(--color-pink) 0%, var(--color-orange) 100%);
    opacity: 0.4;
  }

  .connector-arrow {
    font-size: clamp(32px, 4vw, 48px);
    background: linear-gradient(135deg, var(--color-pink) 0%, var(--color-orange) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0.6;
  }

  .outcome-container {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: var(--space-2xl);
    margin-top: var(--space-xl);
  }

  .outcome-card {
    padding: var(--space-2xl);
    border: 1px solid rgba(255, 107, 157, 0.2);
    background: rgba(255, 255, 255, 0.6);
    border-radius: var(--radius-lg);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(10px);
  }

  .outcome-card:hover {
    transform: translateY(-6px);
    border-color: var(--color-pink);
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 12px 36px rgba(255, 107, 157, 0.2);
  }

  .outcome-card.highlight {
    background: linear-gradient(135deg, rgba(255, 107, 157, 0.1) 0%, rgba(255, 142, 83, 0.1) 100%);
    border-color: var(--color-purple);
    border-width: 2px;
  }

  .outcome-card h4 {
    font-size: clamp(20px, 2.5vw, 32px);
    font-weight: 200;
    color: var(--color-text);
    margin: 0 0 var(--space-md);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .outcome-card p {
    font-size: clamp(16px, 1.8vw, 20px);
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.6;
    font-weight: 200;
  }

  @media (max-width: 968px) {
    .engagement-container {
      flex-direction: column;
      gap: var(--space-xl);
    }

    .meeting-1,
    .meeting-2 {
      margin: 0;
      max-width: 100%;
    }

    .flow-connector {
      transform: rotate(90deg);
      min-width: auto;
      min-height: 80px;
    }

    .connector-line {
      width: 40px;
      height: 2px;
      background: linear-gradient(90deg, var(--color-pink) 0%, var(--color-orange) 100%);
    }

    .outcome-container {
      grid-template-columns: 1fr;
    }

    .pricing-content {
      gap: var(--space-xl);
      padding: 0 var(--space-xl);
      max-height: calc(100vh - 80px);
    }
  }

  @media (max-width: 768px) {
    .meeting-header {
      flex-direction: column;
      gap: var(--space-md);
    }
  }
</style>
