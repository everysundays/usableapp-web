---
// Hero Section - Gigantic Typography
// Full viewport impact with stacked text
---

<section class="hero">
  <div class="container">
    <h1 class="hero-gigantic">
      <span class="giant-line">Find Good</span>
      <span class="giant-line">Problems.</span>
      <span class="line-spacer"></span>
      <span class="giant-line">Make Usable</span>
      <span class="giant-line">Apps.</span>
    </h1>
    <p class="hero-subheadline">
      We help startups and SMEs discover what their business really needs—before writing a single line of code.
    </p>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-white);
    padding: var(--space-4xl) 0;
    position: relative;
    overflow: hidden;
  }

  /* Paintbrush wipe effect */
  .hero::before {
    content: '';
    position: absolute;
    top: -10%;
    left: -5%;
    width: 120%;
    height: 120%;
    background: linear-gradient(
      135deg,
      transparent 0%,
      transparent 30%,
      rgba(212, 165, 116, 0.22) 40%,
      rgba(212, 165, 116, 0.28) 50%,
      rgba(212, 165, 116, 0.22) 60%,
      transparent 70%,
      transparent 100%
    );
    transform: rotate(-8deg);
    pointer-events: none;
    filter: blur(60px);
    opacity: 1;
  }

  /* Secondary subtle wipe */
  .hero::after {
    content: '';
    position: absolute;
    bottom: -5%;
    right: -10%;
    width: 80%;
    height: 50%;
    background: radial-gradient(
      ellipse at bottom right,
      rgba(130, 176, 210, 0.15) 0%,
      rgba(130, 176, 210, 0.20) 30%,
      transparent 70%
    );
    pointer-events: none;
    filter: blur(80px);
    opacity: 0.8;
  }

  .hero .container {
    text-align: center;
    width: 100%;
    padding-left: 0;
    padding-right: 0;
  }

  /* Gigantic Typography Hero */
  .hero-gigantic {
    font-size: clamp(64px, 14vw, 140px);
    font-weight: 700;
    line-height: 0.9;
    letter-spacing: -0.03em;
    color: var(--color-primary);
    margin-bottom: var(--space-3xl);
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .giant-line {
    display: block;
    opacity: 0;
    animation: fadeSlideUp 0.8s ease-out forwards;
  }

  .giant-line:nth-child(1) { animation-delay: 0.1s; }
  .giant-line:nth-child(2) { animation-delay: 0.2s; }
  .giant-line:nth-child(4) { animation-delay: 0.4s; }
  .giant-line:nth-child(5) { animation-delay: 0.5s; }

  .line-spacer {
    display: block;
    height: 0.5em;
  }

  .hero-subheadline {
    font-size: clamp(16px, 2vw, 20px);
    line-height: var(--leading-relaxed);
    color: var(--color-text-light);
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    opacity: 0;
    animation: fadeSlideUp 0.8s ease-out 0.9s forwards;
  }

  @keyframes fadeSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Tablet adjustments */
  @media (max-width: 968px) {
    .hero-gigantic {
      font-size: clamp(64px, 14vw, 160px);
      line-height: 0.9;
    }
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .hero {
      min-height: 80vh;
    }

    .hero-gigantic {
      font-size: clamp(48px, 13vw, 120px);
      line-height: 0.95;
      margin-bottom: var(--space-2xl);
    }
  }

  @media (max-width: 480px) {
    .hero-gigantic {
      font-size: clamp(40px, 14vw, 80px);
      line-height: 1.0;
    }

    .hero-subheadline {
      font-size: 16px;
    }
  }
</style>
