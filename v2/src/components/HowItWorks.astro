---
// How It Works Section
// "Refine the problem. Define the solution."
// Includes Product Surface Foundation diagram
---

<section class="how-it-works section">
  <div class="container">
    <h2 class="big-bold-text">REFINE THE PROBLEM. DEFINE THE SOLUTION.</h2>

    <p class="intro-text">
      Every product is different. Every business has unique patterns. We don't rush discovery—we do it right.
    </p>

    <div class="process-overview">
      <h3>The Process:</h3>
      <ul>
        <li>As many proof-of-concepts as needed (typically 5-7 maximum)</li>
        <li>Each PoC validates one assumption, tests one unknown</li>
        <li>Research-driven, user-validated methodology</li>
        <li>Time-based contract ensures thoroughness</li>
      </ul>
    </div>

    <div class="methodology">
      <h3>Our Methodology:</h3>
      <p class="methodology-intro">
        We build everything from your user requirement down. Underneath the product surface lies a solid foundation, built through testing and iteration.
      </p>

      <div class="methodology-diagram" id="methodology-diagram">
        <!-- Product Surface (Top) -->
        <div class="diagram-layer product-surface">
          <h4>Product Surface</h4>
          <p>What users see and interact with</p>
        </div>

        <!-- Control & Safety Overlay -->
        <div class="diagram-layer overlay-layer">
          <h5>Control & Safety</h5>
          <p>Matched security and safety measures</p>
        </div>

        <!-- Technology & Tools -->
        <div class="diagram-layer tech-layer">
          <h5>Technology & Tools</h5>
          <p class="layer-source">Defined from Operation & System</p>
        </div>

        <!-- User Requirement -->
        <div class="diagram-layer user-req-layer">
          <h5>User Requirement</h5>
          <p>Filled from Goal & Expectations</p>
        </div>

        <!-- Goal & Expectations -->
        <div class="diagram-layer goal-layer">
          <h5>Goal & Expectations</h5>
          <p>Framed to define success</p>
        </div>

        <!-- Foundation (Underneath) -->
        <div class="diagram-layer foundation-layer">
          <h5>Foundation</h5>
          <div class="foundation-items">
            <span class="foundation-item">Problem Statement</span>
            <span class="foundation-item">Workflow Design</span>
            <span class="foundation-item">Data Foundations</span>
          </div>
          <p class="foundation-note">Built through test and tries</p>
        </div>

        <!-- Governance Overview -->
        <div class="diagram-layer governance-layer">
          <h5>Governance | Maintenance | Improvement</h5>
          <p>Continuous oversight and enhancement</p>
        </div>

        <!-- Real-time Data Pillars (Base) -->
        <div class="diagram-layer data-pillars">
          <h5>Real-time Data Pillars</h5>
          <div class="pillar-badges">
            <span class="pillar-badge">Data Management</span>
            <span class="pillar-badge">Data Quality</span>
            <span class="pillar-badge">Privacy & Compliance</span>
            <span class="pillar-badge">Data Stewardship</span>
          </div>
        </div>
      </div>

      <p class="methodology-note">
        Every product is built on validated foundations, not assumptions.
      </p>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    background-color: var(--color-white);
  }

  .how-it-works .big-bold-text {
    color: var(--color-accent-cool);
  }

  .intro-text {
    font-size: var(--text-lg);
    color: var(--color-text);
    max-width: 600px;
    margin: 0 auto var(--space-4xl);
    text-align: center;
  }

  .process-overview {
    background-color: var(--color-hover);
    padding: var(--space-2xl);
    border-radius: var(--radius-lg);
    margin: 0 auto var(--space-4xl);
    border-left: 4px solid var(--color-accent-cool);
    max-width: 800px;
  }

  .process-overview h3 {
    font-size: var(--text-2xl);
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }

  .process-overview ul {
    list-style: none;
    padding: 0;
  }

  .process-overview li {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text);
    padding-left: var(--space-lg);
    position: relative;
    margin-bottom: var(--space-sm);
  }

  .process-overview li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--color-accent-warm);
    font-weight: 700;
  }

  .methodology {
    max-width: 900px;
    margin: var(--space-4xl) auto 0;
  }

  .methodology h3 {
    font-size: var(--text-2xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-lg);
    text-align: center;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .methodology-intro {
    text-align: center;
    font-size: var(--text-base);
    color: var(--color-text);
    margin-bottom: var(--space-3xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Methodology Diagram - Corrected Flow */
  .methodology-diagram {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    margin: var(--space-4xl) 0;
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
  }

  /* Diagram Layer Base Styles */
  .diagram-layer {
    background: var(--color-white);
    padding: var(--space-xl) var(--space-2xl);
    border-radius: var(--radius-lg);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    position: relative;
    transition: all var(--transition-base);
    opacity: 0;
    transform: translateY(30px);
    animation: fadeSlideUp 0.8s ease-out forwards;
  }

  .diagram-layer:nth-child(1) { animation-delay: 0.2s; }
  .diagram-layer:nth-child(2) { animation-delay: 0.3s; }
  .diagram-layer:nth-child(3) { animation-delay: 0.4s; }
  .diagram-layer:nth-child(4) { animation-delay: 0.5s; }
  .diagram-layer:nth-child(5) { animation-delay: 0.6s; }
  .diagram-layer:nth-child(6) { animation-delay: 0.7s; }
  .diagram-layer:nth-child(7) { animation-delay: 0.8s; }
  .diagram-layer:nth-child(8) { animation-delay: 0.9s; }

  .diagram-layer:hover {
    transform: translateY(-4px);
    box-shadow: 0 0 40px rgba(100, 181, 246, 0.3),
                0 8px 32px rgba(0, 0, 0, 0.12);
  }

  /* Product Surface (Top) */
  .product-surface {
    background: var(--color-primary);
    color: var(--color-white);
    text-align: center;
    border-left: 4px solid var(--color-accent-cool);
    box-shadow: 0 0 100px rgba(100, 181, 246, 0.4),
                0 8px 32px rgba(26, 58, 82, 0.3);
  }

  .product-surface h4 {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-white);
    margin-bottom: var(--space-sm);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .product-surface p {
    font-size: var(--text-lg);
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
  }

  /* Overlay Layer (Control & Safety) */
  .overlay-layer {
    border-left: 4px solid var(--color-accent-warm);
    background: linear-gradient(to right, rgba(208, 120, 85, 0.05), var(--color-white));
  }

  .overlay-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .overlay-layer p {
    font-size: var(--text-base);
    color: var(--color-text);
    margin: 0;
  }

  /* Tech Layer */
  .tech-layer {
    border-left: 4px solid var(--color-accent-cool);
  }

  .tech-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .layer-source {
    font-size: var(--text-sm);
    color: var(--color-text-light);
    font-style: italic;
    margin: var(--space-xs) 0 0 0;
  }

  /* User Requirement Layer */
  .user-req-layer {
    border-left: 4px solid var(--color-accent-cool);
    background: linear-gradient(to right, rgba(100, 181, 246, 0.05), var(--color-white));
  }

  .user-req-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .user-req-layer p {
    font-size: var(--text-base);
    color: var(--color-text);
    margin: 0;
  }

  /* Goal Layer */
  .goal-layer {
    border-left: 4px solid var(--color-accent-cool);
  }

  .goal-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .goal-layer p {
    font-size: var(--text-base);
    color: var(--color-text);
    margin: 0;
  }

  /* Foundation Layer (Underneath) */
  .foundation-layer {
    border-left: 4px solid var(--color-accent-warm);
    background: linear-gradient(to right, rgba(208, 120, 85, 0.08), var(--color-white));
  }

  .foundation-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .foundation-items {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-bottom: var(--space-md);
  }

  .foundation-item {
    display: inline-flex;
    padding: var(--space-xs) var(--space-md);
    background: var(--color-accent-warm);
    color: var(--color-white);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .foundation-note {
    font-size: var(--text-sm);
    color: var(--color-text-light);
    font-style: italic;
    margin: 0;
  }

  /* Governance Layer (Overview) */
  .governance-layer {
    border-left: 4px solid var(--color-accent-cool);
    background: linear-gradient(to right, rgba(100, 181, 246, 0.05), var(--color-white));
  }

  .governance-layer h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .governance-layer p {
    font-size: var(--text-base);
    color: var(--color-text);
    margin: 0;
  }

  /* Data Pillars (Base) */
  .data-pillars {
    border-left: 4px solid var(--color-accent-cool);
    background: linear-gradient(to right, rgba(100, 181, 246, 0.08), var(--color-white));
  }

  .data-pillars h5 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .pillar-badges {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .pillar-badge {
    display: inline-flex;
    padding: var(--space-xs) var(--space-md);
    background: transparent;
    border: 2px solid var(--color-accent-cool);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-accent-cool);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all var(--transition-fast);
  }

  .pillar-badge:hover {
    background: var(--color-accent-cool);
    color: var(--color-white);
  }

  @keyframes fadeSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .methodology-note {
    text-align: center;
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-accent-warm);
    margin-top: var(--space-3xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 768px) {
    .methodology-diagram {
      gap: var(--space-sm);
    }

    .diagram-layer {
      padding: var(--space-lg) var(--space-md);
    }

    .product-surface h4 {
      font-size: var(--text-2xl);
    }

    .diagram-layer h5 {
      font-size: var(--text-lg);
    }

    .foundation-items,
    .pillar-badges {
      gap: var(--space-xs);
    }

    .foundation-item,
    .pillar-badge {
      font-size: var(--text-xs);
      padding: 6px var(--space-sm);
    }
  }
</style>

<script>
  // Intersection Observer for scroll-triggered animation
  document.addEventListener('DOMContentLoaded', () => {
    const diagram = document.getElementById('methodology-diagram');

    if (diagram) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              diagram.classList.add('visible');
            }
          });
        },
        { threshold: 0.1 }
      );

      observer.observe(diagram);
    }
  });
</script>
