---
// Showcase Section
// 2 project placeholders
---

<section class="showcase section">
  <div class="container">
    <h2 class="big-bold-text">PROBLEMS SOLVED. APPS THAT WORK.</h2>

    <p class="intro-text">
      Real discoveries. Real results.
    </p>

    <div class="projects">
      <!-- Project 1 - Placeholder -->
      <div class="project">
        <div class="project-image-placeholder">
          <span>Project Image</span>
        </div>
        <div class="project-content">
          <h3 class="project-title">Project Name 1</h3>
          <p class="meta-label">Challenge:</p>
          <p class="meta-description">
            [Brief problem statement - What challenge did the client face?]
          </p>
          <p class="meta-label">Discovery:</p>
          <p class="meta-description">
            [What we uncovered through our research and PoCs]
          </p>
          <p class="meta-label">Result:</p>
          <p class="meta-description">
            [Business outcome and impact]
          </p>
        </div>
      </div>

      <!-- Project 2 - Placeholder -->
      <div class="project">
        <div class="project-image-placeholder">
          <span>Project Image</span>
        </div>
        <div class="project-content">
          <h3 class="project-title">Project Name 2</h3>
          <p class="meta-label">Challenge:</p>
          <p class="meta-description">
            [Brief problem statement - What challenge did the client face?]
          </p>
          <p class="meta-label">Discovery:</p>
          <p class="meta-description">
            [What we uncovered through our research and PoCs]
          </p>
          <p class="meta-label">Result:</p>
          <p class="meta-description">
            [Business outcome and impact]
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .showcase {
    background-color: var(--color-accent-warm);
  }

  .showcase .big-bold-text {
    color: var(--color-white);
  }

  .intro-text {
    font-size: var(--text-lg);
    color: rgba(255, 255, 255, 0.95);
    margin: 0 auto var(--space-4xl);
    text-align: center;
    max-width: 600px;
  }

  .projects {
    display: grid;
    gap: var(--space-4xl);
    max-width: 1100px;
    margin: 0 auto;
  }

  .project {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2xl);
    background-color: var(--color-white);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(26, 58, 82, 0.2);
    transition: all var(--transition-base);
  }

  .project:hover {
    transform: translateY(-6px);
    box-shadow: 0 12px 48px rgba(26, 58, 82, 0.3);
  }

  .project:nth-child(even) {
    grid-template-columns: 1fr 1fr;
  }

  .project-image-placeholder {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 400px;
    color: var(--color-white);
    font-size: var(--text-2xl);
    font-weight: 600;
  }

  .project-content {
    padding: var(--space-3xl);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .project-title {
    font-size: var(--text-2xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-2xl);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .meta-label {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-accent-warm);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: var(--space-xl);
    margin-bottom: var(--space-xs);
  }

  .meta-label:first-of-type {
    margin-top: 0;
  }

  .meta-description {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--color-text);
    margin-bottom: 0;
  }

  @media (max-width: 968px) {
    .project {
      grid-template-columns: 1fr;
    }

    .project:nth-child(even) {
      grid-template-columns: 1fr;
    }

    .project-image-placeholder {
      min-height: 250px;
    }
  }
</style>
